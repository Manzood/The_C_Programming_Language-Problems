#include <stdio.h>

unsigned pow (int p)
{
	int x = 1;
	int ;

	for (i = 0; i < p; i++)
		x *= 2;

	return x;
}

unsigned getbits (unsigned x, int p, int n)
{
	unsigned ans;
	unsigned i, j;

	i = 0;
	j = pow (p);

	ans = x | i;
}

int main()
{

	return 0;
}